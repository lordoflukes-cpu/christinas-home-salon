# Install once: pip install -r requirements.txt
mcp>=1.2
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pytest>=8.0
# The pipeline itself is pure-Python; fastapi/uvicorn are only for the hosted server.
